for grid_size in range(1,10):   
    from functools import lru_cache

    @lru_cache(maxsize=None)
    def paths(x, y):
        if x == grid_size and y == grid_size:
            return 1
        if x > grid_size or y > grid_size:
            return 0
        return paths(x+1, y) + paths(x, y+1)

    print(paths(0,0))

#Very happy with this code, clean and optimised. I researched caching then used it. Also I saw on the thread that it follows pascals triangle and was fascinated so decided to look into it!
